AI used to resolve issue with Android version and viewModel() library. though it broke something to do with rendering after making changes to the gradle files. was not able to fix that. preview interactive mode works, but can't run it on my emulator
